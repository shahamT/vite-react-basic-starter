// === Style

// === Libs

// === Services

// === Actions

// === Hooks / React

// === Imgs

// === Child Components

// ====== Component ======
// =======================

export function HomePage({ }) {
    // === Consts

    // === Effects

    // === Functions

    return (
        <section className="HomePage full-width subgrid">
            <h1>HomePage</h1> 
        </section>
    )
}