// === Style

// === Libs

// === Services

// === Actions

// === Hooks / React

// === Imgs

// === Child Components

// ====== Component ======
// =======================

export function AppFooter({ }) {
    // === Consts

    // === Effects

    // === Functions

    return (
        <section className="AppFooter full-width subgrid">
            <h1>AppFooter</h1>
        </section>
    )
}