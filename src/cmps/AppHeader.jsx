// === Style

// === Libs

// === Services

// === Actions

// === Hooks / React

// === Imgs

// === Child Components

// ====== Component ======
// =======================

export function AppHeader({ }) {
    // === Consts

    // === Effects

    // === Functions

    return (
        <section className="AppHeader full-width subgrid">
            <h1>AppHeader</h1>
        </section>
    )
}